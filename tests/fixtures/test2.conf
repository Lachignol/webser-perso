server {
    #listen -1;                       # Erreur : port négatif
    #host 127.0.0.1a;                 # Erreur : IP invalide
    #server_name local host;          # Erreur : espace dans le nom
    #error_page 399 /errors/404.html; # Erreur : code hors plage
    #client_max_body_size 0M;         # Erreur : taille nulle

    location /html {                  # Erreur : ne commence pas par /
        #root /var/www/ht..ml;        # Erreur : ".." dans le chemin
        #index /index.html;           # Erreur : index contient un /
        #autoindex maybe;             # Erreur : valeur non reconnue
        #allowed_methods GET PUT;     # Erreur : "PUT" non autorisé
    }

    location /upload {                # Erreur : ne commence pas par /
        #root ;                      # BBUUUUGGGG Erreur : chemin vide
        #allowed_methods PATCH;       # Erreur : méthode non autorisée
        #upload_enable yes;           # Erreur : valeur non reconnue
        # upload_store ;               # BBBUUUGGG Erreur : chemin vide
    }
}

server {
    #listen 70000;                   # Erreur : port trop grand
    # host 127.0.0.1.1;               # BBBUUUGGG Erreur : IP invalide
    # server_name test.local!;         # BBBUUUUGGG Erreur : caractère interdit
    # error_page 700 /errors/404.html; # Erreur : code hors plage
    client_max_body_size 1A;       # Erreur : taille trop grande

    location / {                     
        #root /var/www/te..stsite;   # Erreur : ".." dans le chemin
        #index home/html;            # Erreur : index contient un /
        #autoindex 1;                # Erreur : valeur non reconnue
        #allowed_methods GET HEAD;   # Erreur : "HEAD" non autorisée
        #cgi_extension py;           # Erreur : ne commence pas par .
        cgi_path ;                  # Erreur : chemin vide
    }

    location /redirect {              # Erreur : ne commence pas par /
        #return 99 localhost;        # Erreur : code de retour invalide
    }
}
